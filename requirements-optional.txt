# Optional, heavier PII detection backend.
# The app ships with a dependency-free regex detector and works without these.
# Install with:
#   pip install -r requirements-optional.txt
#   python -m spacy download en_core_web_sm
presidio-analyzer==2.2.360
presidio-anonymizer==2.2.360
spacy==3.8.7
