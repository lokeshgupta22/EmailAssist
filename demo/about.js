/*
 * The demo's About page. Its only behaviour is the History list, which is
 * shared with the Tasks page in history.js and built from the same recorded
 * results.json the workspace uses. Clicking a row navigates to index.html
 * with that thread preloaded, since the analysis itself is only ever shown
 * there.
 */

"use strict";

loadDemoHistory();
